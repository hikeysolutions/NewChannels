# Arc Bureau — References

## Canonical Creative Spec
The locked Arc Bureau master system prompt lives in [ARC_BUREAU_SPEC.md](./ARC_BUREAU_SPEC.md) —
source of truth for the Analyst persona, stage structure, change categories, and hard constraints.
CLAUDE.md summarizes it; the spec file is authoritative.

## Seeded Concept Bank (candidate ideas only — NOT database rows)

Nine seed concepts logged here as candidate ideas for Arc Bureau. These are candidates only. No
`entity_situation_bank` rows are populated from this list — populating the actual bank is a separate
decision, out of scope for the scaffolding pass.

All seeds are archetypal / composite by construction. No real, identifiable named individuals, per
the Part 4 no-real-person rule (which applies to narration/narrative content, not just visuals — see
tasks/lessons.md and CLAUDE.md).

1. astronaut
2. alien
3. sudden heart attack
4. gunslinger
5. gold-rush-era cowboy
6. extreme / exotic travel
7. poor in LA
8. middle class in Detroit
9. homeless in Alaska

> Note: how these map to the channel's stage/change structure — and whether Arc Bureau's
> change-categories become their own taxonomy or reuse the existing `gap_type` enum — is one of the
> OPEN/UNRESOLVED items pending a Layer 2 planning review (see CLAUDE.md). Do not build a bank schema
> around these until that is resolved.
